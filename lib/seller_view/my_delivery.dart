import 'package:flutter/material.dart';

class MyDelivery extends StatelessWidget {
  const MyDelivery({super.key});

  @override
  Widget build(BuildContext context) {
    return  Scaffold(
      body: Center(child: Text("My Delivery")),
    );
  }
}