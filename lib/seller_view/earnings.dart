import 'package:flutter/material.dart';

class MyEarnings extends StatelessWidget {
  const MyEarnings({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child: Text("My Earnings")),
    );
  }
}